/**
 * S3 — name, district, role. The `register` call.
 *
 * P0 placeholder. P1 builds it for real.
 *
 * ★ I9 when you do. There is no Aadhaar field on this screen, there is no Aadhaar
 *   field anywhere, and there is no "optional" version of one. Phone is the
 *   identifier. If a stakeholder asks for Aadhaar during the demo, the answer is
 *   that we deliberately do not collect it — not that we will add it later.
 */

import React from 'react';
import { Soon } from '../Soon';

export default function S03_Profile() {
  return <Soon label="S3 · तुमची माहिती" />;
}
