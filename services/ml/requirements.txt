# MANDI-SETU ML service.  OWNER: R2.
#
# Install with Python 3.11 — NOT the system python3 (3.14, no LightGBM wheel exists):
#   uv venv --python 3.11
#   uv pip install -r requirements.txt
#
# Versions are pinned. An unpinned resolve at H60 that pulls a breaking numpy
# is a self-inflicted wound.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.0
lightgbm==4.5.0
statsmodels==0.14.4
httpx==0.28.1
python-dotenv==1.0.1

# dev
pytest==8.3.4
ruff==0.8.6
